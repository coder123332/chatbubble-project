import React from 'react';

const NavBar = () => (
  <nav className="navbar">
    <h1 className="logo">Pixel Bubble</h1>
    <a className="shop-btn" href="/shop">Go to Shop</a>
  </nav>
);

export default NavBar;
